# L{CORE} Environment Configuration
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# See docs/CONFIGURATION.md for detailed documentation.

# ============================================
# REQUIRED - You must set these values
# ============================================

# Wallet private key (for signing blockchain transactions)
# Generate: node -e "console.log('0x' + require('crypto').randomBytes(32).toString('hex'))"
PRIVATE_KEY=

# Or use mnemonic instead of PRIVATE_KEY
# MNEMONIC=

# Blockchain RPC endpoint (Arbitrum Sepolia)
# Get one from: Alchemy, Infura, QuickNode, etc.
LCORE_RPC_URL=https://arb-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# WebSocket endpoint (optional, for real-time events)
CARTESI_WS_ENDPOINT=wss://arb-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# Cartesi DApp address (from `cartesi deploy` output)
# Deploy your own: cd cartesi && cartesi build && cartesi deploy --network arbitrum-sepolia
LCORE_DAPP_ADDRESS=

# ============================================
# ENCRYPTION KEYS - Generate these
# ============================================

# Generate NaCl keypair:
# node -e "const nacl = require('tweetnacl'); const kp = nacl.box.keyPair(); console.log('LCORE_ADMIN_PUBLIC_KEY=' + Buffer.from(kp.publicKey).toString('base64')); console.log('LCORE_ADMIN_PRIVATE_KEY=' + Buffer.from(kp.secretKey).toString('base64'));"

LCORE_ADMIN_PUBLIC_KEY=
LCORE_ADMIN_PRIVATE_KEY=

# Proof signing key (32-byte hex)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PROOF_SIGNING_KEY=

# ============================================
# OPTIONAL - Defaults are usually fine
# ============================================

# InputBox contract address (standard for Arbitrum Sepolia)
LCORE_INPUTBOX_ADDRESS=0x59b22D57D4f067708AB0c00552767405926dc768

# Blockchain chain ID (421614 = Arbitrum Sepolia)
CARTESI_BLOCKCHAIN_ID=421614

# Log level (debug, info, warn, error)
LOG_LEVEL=info
