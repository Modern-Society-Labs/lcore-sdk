# L{CORE} Attestor Configuration
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# For docker-compose deployment, use the root .env.example instead.
# This file is for standalone attestor deployment.

# =============================================================================
# SERVER
# =============================================================================
PORT=8001
LOG_LEVEL=info
NODE_ENV=production
CORS_ALLOWED_ORIGINS=*

# =============================================================================
# WALLET (Required)
# =============================================================================
# Option 1: Mnemonic phrase (recommended for EigenCloud TEE)
MNEMONIC=

# Option 2: Direct private key (hex format, with or without 0x prefix)
# PRIVATE_KEY=

# =============================================================================
# L{CORE} CONFIGURATION (Required)
# =============================================================================
# Enable L{CORE} integration
LCORE_ENABLED=1

# Cartesi Node URL (for inspect queries)
# Example: http://localhost:10000 or http://your-cartesi-node:10000
LCORE_NODE_URL=

# Blockchain RPC URL (Arbitrum Sepolia)
# Get from: Alchemy, Infura, QuickNode
LCORE_RPC_URL=https://arb-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# DApp contract address (from `cartesi deploy` output)
LCORE_DAPP_ADDRESS=

# InputBox contract address (standard for Arbitrum Sepolia)
LCORE_INPUTBOX_ADDRESS=0x59b22D57D4f067708AB0c00552767405926dc768

# =============================================================================
# INPUT ENCRYPTION (Required for Device Attestation)
# =============================================================================
# Public key for encrypting device inputs before blockchain submission.
# The matching private key must be baked into the Cartesi Dockerfile.
#
# Generate keypair:
# node -e "const nacl=require('tweetnacl');const kp=nacl.box.keyPair();console.log('LCORE_INPUT_PUBLIC_KEY='+Buffer.from(kp.publicKey).toString('base64'));console.log('LCORE_INPUT_PRIVATE_KEY='+Buffer.from(kp.secretKey).toString('base64'))"
#
# Put PUBLIC key here, PRIVATE key in cartesi/Dockerfile
LCORE_INPUT_PUBLIC_KEY=

# =============================================================================
# OUTPUT ENCRYPTION (Optional)
# =============================================================================
# For decrypting Cartesi query responses (if output encryption is enabled).
#
# Generate keypair (same command, different keys):
# node -e "const nacl=require('tweetnacl');const kp=nacl.box.keyPair();console.log('PUBLIC='+Buffer.from(kp.publicKey).toString('base64'));console.log('PRIVATE='+Buffer.from(kp.secretKey).toString('base64'))"
#
# Put PRIVATE key here, PUBLIC key in cartesi/Dockerfile
LCORE_ADMIN_PRIVATE_KEY=
LCORE_ADMIN_PUBLIC_KEY=
